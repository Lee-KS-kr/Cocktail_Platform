	<!DOCTYPE mapper
        	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
       		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.platform.cocktail.cocktail_platform.dao.RecommendDAO">

	<select id="getTop3Cocktails" resultType="Cocktails">
 		select
 			c.cocktailName as cocktailName
		from
			cocktails c
				left join
					orderTemp ot
				on
					c.cocktailName = ot.menuName
		group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>
	 
	<select id="customizedRecommendation" parameterType="Taste" resultType="Cocktails">
		select
    		c.cocktailName
		from
    		cocktails c
				inner join taste t on c.cocktailTaste = #{cocktailTaste} or c.cocktailFlavor = #{cocktailFlavor}
				left join orderTemp ot on c.cocktailName = ot.menuName
    	where
    		t.memberId = #{memberId}
    	group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>

	
    <select id="weatherRecommend" parameterType="int" resultType="Cocktails">
 		select
 			c.cocktailName as cocktailName
		from
			cocktails c
				inner join menuPreference mp on	c.cocktailName = mp.menuName
				left join orderTemp ot on c.cocktailName = ot.menuName
		where
			mp.weather = #{weather}
		group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>
    
	<select id="ageRecommend" parameterType="int" resultType="Cocktails">
 		select
 			c.cocktailName as cocktailName
		from
			cocktails c
				inner join menuPreference mp on	c.cocktailName = mp.menuName
				left join orderTemp ot on c.cocktailName = ot.menuName
		where
			mp.ageGroup = #{ageGroup}
		group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>

	<select id="companionRecommend" parameterType="int" resultType="Cocktails">
 		select
 			c.cocktailName as cocktailName
		from
			cocktails c
				inner join menuPreference mp on	c.cocktailName = mp.menuName
				left join orderTemp ot on c.cocktailName = ot.menuName
		where
			mp.companion = #{companion}
		group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>

	<select id="eventRecommend" parameterType="int" resultType="Cocktails">
		select
 			c.cocktailName as cocktailName
		from
			cocktails c
				inner join menuPreference mp on	c.cocktailName = mp.menuName
				left join orderTemp ot on c.cocktailName = ot.menuName
		where
			mp.event = #{event}
		group by
			c.cocktailName
		order by
			sum(ot.orderCount) desc
	</select>
	
	<select id="getCocktailByName" parameterType="String" resultType="Cocktails">
    	select
    		*
    	from
    		cocktails
    	where
    		cocktailName = #{cocktailName}
	</select>
	
	<select id="recommendForMe" parameterType="String" resultType="Cocktails">
    	select
        	c.cocktailName as cocktailName
    	from
        	cocktails c
        	left join orderTemp ot on c.cocktailName = ot.menuName
        where
    		ot.memberId = #{memberId}
    	group by
        	c.cocktailName
    	order by
        	sum(ot.orderCount) desc
	</select>
	
	</mapper>