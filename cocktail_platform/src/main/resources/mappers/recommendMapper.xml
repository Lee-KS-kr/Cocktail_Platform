	<!DOCTYPE mapper
        	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
       		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.platform.cocktail.cocktail_platform.dao.RecommendDAO">

	<!-- 인기상품
	<select id="getTop3Cocktails" parameterType="" resultType="">
		select 
    		menuName
    		, sum(orderCount) totalOrderCount
    		, dense_rank() over (order by sum(orderCount) desc) menuRank
		from
    		orderTemp
		group by
    		menuName
		order by
    		menuRank asc;
	</select>
	 -->

	<!-- 님을 위한 추천 상품 -->


	<!-- ㅇㅇ님과 비슷한 20대가 많이 찾아요 -->

	<!-- 오늘 같이 비가 오는 날에는? -->


	<!-- 친구와 많이 마셔요 -->

	<!-- 특별한 순간을 즐기고 싶을 떄는? -->

	</mapper>